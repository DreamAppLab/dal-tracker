{
  "buildCommand": "npm ci && DISABLE_ESLINT_PLUGIN=true npm run build",
  "installCommand": "npm ci",
  "rewrites": [
    { "source": "/api/stripe-webhook", "destination": "/api/stripe-webhook.js" }
  ]
}
